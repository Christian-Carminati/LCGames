# Piano Implementazione LCGames

## 1. Aggiungere Variabile Difficoltà PAL/NTSC

### Obiettivo
Implementare un sistema dove l'admin può configurare un indirizzo di memoria per la selezione PAL/NTSC (valori: 00=PAL, 01=NTSC), che influenza la difficoltà del gioco.

### Implementazione

#### 1.1 Modificare Prisma Schema
- Aggiungere campo `palNtscConfig` al modello `Game` in `prisma/schema.prisma`:
```prisma
palNtscConfig Json?  // { "address": "0x2xxx", "baseOffset": "0x0", "numStandards": 2 }
```

#### 1.2 Modificare Validations
- Aggiornare `src/lib/validations.ts` per accettare il nuovo campo nel `GameSchema`.

#### 1.3 Modificare Admin API
- Modificare `src/app/api/admin/games/route.ts` per salvare il nuovo campo.
- Modificare `src/app/api/admin/games/[slug]/route.ts` per restituire il campo.

#### 1.4 Modificare Emulator Component
- Modificare `src/components/Emulator.tsx` per:
  - Ricevere `palNtscConfig` come prop
  - Passarlo all'emulatore via URL params
  - Leggere il valore di memoria (0=PAL, 1=NTSC)

#### 1.5 Modificare GameInterface
- Modificare `src/components/GameInterface.tsx` per:
  - Ricevere `palNtscConfig` dal page.tsx
  - Passarlo all'Emulator

#### 1.6 Modificare Game Page
- Modificare `src/app/games/[slug]/page.tsx` per:
  - Ottenere `palNtscConfig` dal database
  - Passarlo a GameInterface

#### 1.7 Modificare ScoreBoard per supportare PAL/NTSC
- Modificare `src/components/ScoreBoard.tsx` per:
  - Ricevere `palNtscConfig` come prop
  - Calcolare la difficoltà corretta: `baseDifficulty + (isNTSC ? numDifficultyLevels : 0)`
  - Mostrare selettore PAL/NTSC nell'UI

#### 1.8 Aggiornare Admin UI
- Modificare il form admin per permettere l'inserimento dell'indirizzo di memoria PAL/NTSC.

---

## 2. Verifica Cheat nel LocalStorage

### Obiettivo
Rifiutare i punteggi se vengono rilevati cheat attivi nel localStorage.

### Implementazione

#### 2.1 Creare funzione di verifica cheat
- Creare `src/lib/cheat-detection.ts`:
```typescript
export function checkCheatsEnabled(romPath: string): { hasCheats: boolean; cheatCount: number } {
  if (typeof window === 'undefined' || !romPath) {
    return { hasCheats: false, cheatCount: 0 };
  }
  
  const settingsKey = `ejs-1-c64-${romPath}-settings`;
  try {
    const settings = JSON.parse(localStorage.getItem(settingsKey) || '{}');
    const cheats = settings.cheats || [];
    return { hasCheats: cheats.length > 0, cheatCount: cheats.length };
  } catch {
    return { hasCheats: false, cheatCount: 0 };
  }
}
```

#### 2.2 Modificare ScoreBoard
- Importare e usare la funzione `checkCheatsEnabled` nel `handleSaveScore`.
- Mostrare errore se `hasCheats` è true:
```typescript
if (hasCheats) {
  showNotification("Cheats detected! Please disable them before saving score.", "error");
  return;
}
```

#### 2.3 Modificare API scores
- Anche in `src/app/api/scores/route.ts` aggiungere verifica per sicurezza lato server.

---

## 3. Download ROM Solo per Admin

### Obiettivo
Nascondere completamente il pulsante Download ROM per utenti non-admin.

### Implementazione

#### 3.1 Modificare GameInterface
- Modificare `src/components/GameInterface.tsx` lines 312-325:
```tsx
{isAdmin && romPath && (
  <div className="nes-container is-rounded is-dark with-title">
    <p className="title">ACTIONS</p>
    <div className="flex flex-col gap-3">
      <a href={romPath} download className="nes-btn is-success w-full">
        DOWNLOAD ROM
      </a>
    </div>
  </div>
)}
```

---

## 4. Separare CurrentScore dalla Leaderboard

### Obiettivo
Creare due card separate: una per il punteggio attuale (Current Score) e una per la classifica (Leaderboard).

### Implementazione

#### 4.1 Creare nuovo componente CurrentScoreCard
- Creare `src/components/CurrentScoreCard.tsx`:
```typescript
'use client';

import { useState, useEffect } from 'react';
import { useSession, signIn } from 'next-auth/react';
import { useNotification } from '@/context/NotificationContext';
import { generateScoreHash } from '@/lib/security';

interface CurrentScoreCardProps {
  gameSlug: string;
  capturedScore?: number;
  hasDifficultyLevels?: boolean;
  currentDifficulty?: number;
  difficultyNames?: string[];
  hasPalNtsc?: boolean;
  currentStandard?: 'PAL' | 'NTSC';
  numDifficultyLevels?: number;
}

export function CurrentScoreCard({ ... }: CurrentScoreCardProps) {
  // Logica simile a ScoreBoard ma solo per il punteggio corrente
  // Include: display punteggio, selezione difficoltà, pulsante save/login
}
```

#### 4.2 Modificare ScoreBoard per essere solo Leaderboard
- Rinominarlo in `LeaderboardCard.tsx` o mantenere il nome ma senza la sezione Current Score.
- Rimuovere tutto il codice relativo a `capturedScore`, `handleSaveScore`, etc.

#### 4.3 Modificare GameInterface
- Separare i due componenti:
```tsx
{/* Current Score Card */}
<CurrentScoreCard 
  gameSlug={gameSlug}
  capturedScore={currentScore}
  hasDifficultyLevels={!!difficultyConfig}
  currentDifficulty={currentDifficulty}
  difficultyNames={difficultyConfig?.levelNames || []}
/>

{/* Leaderboard Card */}
<ScoreBoard 
  gameSlug={gameSlug}
  capturedScore={undefined}
  isAutoTracked={false}
/>
```

---

## File da Modificare/Creare

| File | Azione |
|------|--------|
| `prisma/schema.prisma` | Modifica |
| `src/lib/validations.ts` | Modifica |
| `src/lib/cheat-detection.ts` | Nuovo |
| `src/app/api/admin/games/route.ts` | Modifica |
| `src/app/api/admin/games/[slug]/route.ts` | Modifica |
| `src/app/api/scores/route.ts` | Modifica |
| `src/components/Emulator.tsx` | Modifica |
| `src/components/GameInterface.tsx` | Modifica |
| `src/components/ScoreBoard.tsx` | Modifica |
| `src/components/CurrentScoreCard.tsx` | Nuovo |
| `src/app/games/[slug]/page.tsx` | Modifica |
| Admin game form | Modifica |

---

## Note Aggiuntive

- La formula per la difficoltà combinata (difficulty + PAL/NTSC) sarà:
  - Per `N` livelli di difficoltà e PAL: `1, 2, 3, ..., N`
  - Per `N` livelli di difficoltà e NTSC: `N+1, N+2, N+3, ..., N+N`
- Esempio con 3 livelli: Easy PAL=1, Normal PAL=2, Hard PAL=3, Easy NTSC=4, Normal NTSC=5, Hard NTSC=6
